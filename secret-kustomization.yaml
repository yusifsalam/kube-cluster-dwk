apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: my-secrets
  namespace: flux-system
spec:
  interval: 10m0s
  sourceRef:
    kind: GitRepository
    name: main-repo
  path: ./ping-pong/manifests
  prune: true
  decryption:
    provider: sops
    secretRef:
      name: sops-age
